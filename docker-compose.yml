services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shipment_tracker
    ports:
      - "5432:5432"
    volumes:
      - shipment_tracker_pgdata:/var/lib/postgresql/data

volumes:
  shipment_tracker_pgdata:
